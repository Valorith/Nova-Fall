# ===========================================
# Nova Fall Frontend - Environment Configuration
# ===========================================
# These are BUILD-TIME variables. They are baked into the bundle during build.
# After changing these in Railway, you MUST redeploy to rebuild.

# ===========================================
# API URL (REQUIRED in production!)
# ===========================================
# The URL of the backend API server.
# Used for: OAuth login redirects, API requests, token refresh
#
# Local development:
VITE_API_URL=http://localhost:3000

# Railway production (set in Railway Frontend service variables):
# VITE_API_URL=https://nova-fall-api-production.up.railway.app
#
# IMPORTANT: If not set correctly, OAuth will redirect to localhost!

# ===========================================
# WebSocket URL (REQUIRED for real-time features)
# ===========================================
# The URL of the WebSocket server.
# Used for: Real-time node updates, battle events
#
# Local development:
VITE_WS_URL=http://localhost:3002

# Railway production (set in Railway Frontend service variables):
# VITE_WS_URL=https://nova-fall-ws-production.up.railway.app
