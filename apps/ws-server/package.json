{
  "name": "@nova-fall/ws-server",
  "version": "0.1.0",
  "private": true,
  "description": "WebSocket server for real-time game events",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nova-fall/shared": "workspace:*",
    "socket.io": "^4.7.0",
    "ioredis": "^5.3.0",
    "pino": "^8.17.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "pino-pretty": "^11.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
